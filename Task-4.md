# Setir boşmy diýip barla (length === 0).
```
function empty(text) {
    if (text.length==0) {
       return "Bosh"
    }   else {
            return"Doly" 
            
    }
}
console.log(empty(" "));
console.log(empty("RandomText"));
```
